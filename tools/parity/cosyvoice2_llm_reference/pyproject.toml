[project]
name = "cosyvoice2-llm-reference"
version = "0.1.0"
requires-python = "==3.12.*"
dependencies = [
    "numpy==2.3.5",
    "torch==2.7.1",
    "transformers==5.10.4",
]

[tool.uv]
package = false
environments = ["sys_platform == 'linux' and platform_machine == 'x86_64'"]

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true

[tool.uv.sources]
torch = { index = "pytorch-cpu" }
