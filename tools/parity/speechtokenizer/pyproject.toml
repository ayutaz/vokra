[project]
name = "vokra-speechtokenizer-parity"
version = "0.1.0"
description = "Pinned official SpeechTokenizer token-to-waveform reference; VAST only"
requires-python = ">=3.12,<3.13"
dependencies = [
    "einops==0.8.2",
    "numpy==2.1.3",
    "torch==2.13.0",
]
