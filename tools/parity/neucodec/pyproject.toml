[project]
name = "vokra-neucodec-parity"
version = "0.1.0"
description = "Pinned official NeuCodec token-to-waveform parity oracle; never shipped in the runtime."
requires-python = ">=3.12,<3.13"
dependencies = [
    "einops==0.8.2",
    "gguf==0.19.0",
    "numpy>=2.0.2",
    "torch==2.13.0",
    "torchtune==0.3.1",
    "vector-quantize-pytorch==1.17.8",
]

[tool.uv]
package = false
