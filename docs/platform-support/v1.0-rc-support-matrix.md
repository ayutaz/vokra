# v1.0-rc Platform Support Confirmation Matrix (M4-11)

> **本ファイルは M4-11 の主成果物 = 6 プラットフォーム official support の確認・記録。**
> **確認 WP であって実装 WP ではない** (spec: `docs/tickets/m4/M4-11-all-platform-support.md`)。
> gap セルを隠して「確認済」と宣言しない (**fabricated pass 禁止** — `feedback-honest-parity-atol.md` の一般化)。
> 空の evidence セル・owner 記入枠は `docs/benchmarks/v0.9-device-benchmarks.md` と同じ分担哲学: 実機/選定は依頼者、確認 matrix の機械化と evidence 収集は CC。

**対象要件**: NFR-PT-01 (全プラットフォーム対応前提) / NFR-MT-02 (CI matrix tier) / NFR-DS-03 (配布パッケージ) / BR-02 (マルチプラットフォーム安定性)。
**v-label 規律** (2026-07-14 再割当 #2): M4 = **v1.0-rc** (M5 = v1.0 GA)。本確認記録は「v1 の機能完成 (rc) = 全プラットフォーム official support 出揃い」の判定材料であり、**商用 GA 宣言・C ABI 凍結・semver 準拠開始のいずれでもない** (凍結発火は M5-13/v1.0 GA タグ)。
**drift check**: 本文書が cite する evidence anchor (HTML コメント形式、`anchor:` プレフィックスの行) の実在は `scripts/check-platform-support.sh` が機械検証する (T08)。anchor token は必ず repo 相対パス (`path` または `path#job`) で、`scripts/check-platform-support.sh --list` で列挙できる。

---

## Part 0 — HEAD スナップショット (honest snapshot)

### 0.1 初版観測 (起票時点 base HEAD `328e61b` = M3-terminal + docs)

本 matrix 初版は **その時点の実 repo 状態を機械確認して作成**した (発明ゼロ)。初版が切られた worktree は M4 の統合 branch ではなく **base HEAD `328e61b`** から分岐していたため、下記は M4 Wave 1-3 land **前**の観測である (§0.2 で更新):

- **Web (WASM / WebGPU / npm) の build/CI/実機 leg はゼロ**: `.github/workflows/` に wasm / webgpu / wgpu / emscripten の job 記述なし、`web/` ディレクトリ・npm `package.json` (Unity UPM 用を除く) なし。→ 初版では **Web 行は全面 gap** と記録。owning WP = M4-01 (WASM/WebGPU/npm) + M4-02 (Unity WebGL)。
- **Vulkan** は M3-02 scaffold + `gpu-vulkan-parity.yml` (workflow_dispatch + weekly、required check 非昇格) 段階。完成は M4-13 のスコープ。
- **default test baseline = 1759 passed / 0 failed** (M4 Wave 1-3 が base に無いことと整合)。
- desktop 3 OS / iOS / Android / server ISA / RISC-V の CI・配布資産は land 済 (下記各行の anchor 参照)。

### 0.2 統合 HEAD 実態への更新 (2026-07-15、統合 HEAD `0691ae9` = M4 Wave 1-3 land 済)

初版の worktree が古い base から切られていたため Web / Vulkan を「未 land」と誤記録していた。**現在の統合 HEAD には M4 Wave 1-3 の全成果が入っている**。以下を各 anchor で実在再確認し、Part 4–8 / 11 を実態へ更新した (**fabricated pass 禁止** = CC-done と owner-pending を honest に分離):

- **Web (M4-01 WebGPU + M4-02 Unity WebGL) は CC-side land 済**:
  - WebGPU backend crate `crates/vokra-backend-webgpu/` (11 WGSL kernel + JS glue + probe/plan/eval)。
  - npm パッケージ足場 `web/pkg/package.json` (+ `index.js` / `index.d.ts`) + demo `web/`。
  - CI leg `web-wasm.yml` (job = `wasm-build` / `whisper-wasm-e2e` / `webgpu-smoke`、dispatch + weekly cron + PR path filter) + `nightly-webgl.yml` (Unity WebGL、nightly cron、`UNITY_LICENSE` gated)。
  - npm CD `release.yml#npm-web-release` (tag で GitHub Release tarball auto、registry publish は `NPM_TOKEN` opt-in)。
  - Unity WebGL は `wasm32-unknown-emscripten` staticlib (`scripts/build-unity-webgl-lib.sh`) を UPM `Plugins/WebGL/` に配置 (Unity 同梱 Emscripten がリンク = 別 `.jslib` glue は不使用)。
  - **owner-pending は継続 gap**: browser 実機テスト (T11) + CDN 選定 (T12) + registry publish opt-in の実発行。
- **Vulkan (M4-13) は CC-side land 済だが実 `.spv` kernel は owner glslc 待ち = partial**: backend crate `crates/vokra-backend-vulkan/` + GLSL source 12 本 (`kernels/glsl/*.comp`) + `gpu-vulkan-parity.yml`。ただし `kernels/precompiled/` は README のみで、committed 済の実 SPIR-V は handcrafted の `copy_f32` / `add_f32` の 2 本のみ (MatMul / Mul / Softmax 等は owner の glslc precompile = M4-13-T16 / M3-02 T14-T22 待ち)。
- **default test baseline = 2418 passed / 0 failed / 4 ignored** (`cargo test --workspace`、2026-07-16 実測。M4 terminal 時 2340（2026-07-15、初版 1759 から M4 Wave 1-3 分 +581）→ post-terminal CC-gap 追加実装 campaign で +78 = 2418。all-features `vulkan,metal,cuda` = 2443。新規 C ABI なし = baseline 33 fn 不変)。
- **その他 Wave 1-3 land 済** (本 matrix の 6 プラットフォーム判定には非依存の補助): CSM/Moshi (`crates/vokra-models/src/{csm,moshi,mimi}/`)、FSQ/RVQ/aec/agc/hpf/loudness_norm/denoise op (`crates/vokra-ops/src/`)、CPU ISA server tier (`crates/vokra-backend-cpu`)、FA v3 (`crates/vokra-backend-cuda`)、対応 parity workflow (`parity-csm-real.yml` / `parity-moshi-real.yml` / `parity-rvq-real.yml` / `parity-utmos.yml`)。

> **判定含意 (更新後)**: Web 行の CI leg + 配布 CD は緑化した (CC-side)。**Web 行が完全緑になるのは owner Web 実機 (T11) + CDN 選定 (T12) が記録された後のみ** = M4-11 の close 条件は owner sign-off (T13) 待ちで未充足のまま。Vulkan は partial (owner glslc 後に実 op parity が緑化)。

---

## Part 1 — 「official support」の operational 定義 (T01)

「official support」は**新規発明ではなく既存要件の合成**である。プラットフォーム P が official support であるとは、以下 3 evidence が揃うこと:

| # | evidence 列 | 定義 | 出典 |
|---|------------|------|------|
| (i) | **CI 検証** | NFR-MT-02 の該当 tier で P 向け build/cross-build/test が継続実行され green。required checks は NFR-MT-07。 | NFR-MT-02 / NFR-MT-07 / NFR-PT-01 |
| (ii) | **配布パッケージ** | `docs/deliverables.md` §3.2 の P 行の形態が NFR-MT-08 の CD 自動発行で得られる (手動ビルド配布は不可)。 | NFR-DS-03 / NFR-MT-08 / deliverables §3.2 |
| (iii) | **実機検証記録** | owner の実機テスト記録が `docs/benchmarks/` 系文書に存在する。 | NFR-PF-03/06/08 / X-06 |

**3 列構成の妥当性**: milestones §8 M4-11 完了条件「6 プラットフォームの official support が **CI matrix (NFR-MT-02) と配布パッケージ (NFR-DS-03)** で確認でき、CDN 選定が完了」+ Exit criteria「M4-11 の確認記録」に対応。(i)(ii) は完了条件の明示 2 軸、(iii) は Exit criteria「全プラットフォーム official support 出揃い」の実証裏付け。

**判定対象は 6 プラットフォームのみ** (NFR-PT-01 前段 = Windows / macOS / Linux / Android / iOS / Web (WASM))。サーバ x86-64/ARM64 は desktop Linux/Windows と CI 上同一 evidence で覆われ、RISC-V (`riscv-cross-build`) や IoT 階層 (NFR-PT-02/03) は判定対象外の**補助 evidence** として付録 (Part 6) に記録する (判定に混ぜない)。

**配布パッケージの判定基準を拡大解釈しない**: 完了条件「配布パッケージ (NFR-DS-03) で確認」の判定基準は **deliverables §3.2 のプラットフォーム別形態**であって、NFR-DS-03 が列挙する全 11 形態 (vcpkg/Homebrew/winget/Debian 系まで) の完備ではない。パッケージマネージャ流通は判定対象外の付録チャネル (Part 4 で分離)。

---

## Part 2 — gap flow 規約 (T01)

確認 matrix は「全部緑になるまで作らない」のではなく、**現状の honest snapshot を先行作成し gap セルを可視化したまま順次緑化する**。赤セルの処理:

1. **owning WP が M4 内に存在** → その WP へ差し戻す (例: Web CI leg = M4-01、Unity WebGL = M4-02、Vulkan 完成 = M4-13)。
2. **owning WP 不在** → issue 起票 + T13 で owner が 3 択判断:
   - (a) M4 内 follow-up ticket を新設、
   - (b) M5 (v1.0 GA) へ送る、
   - (c) 要件側 (deliverables §3.2 / NFR-DS-03) を改訂する。

**gap を残したまま matrix を「確認済」と close しない** (fabricated pass 禁止)。confirm セルは evidence (workflow run URL / 配布物 URL / 実機記録) が存在するものだけ緑。到達していない NFR-MT-02 tier を到達済みと書かない。

**FR-EX-08 の精神**: 「動かないプラットフォームを official と宣言しない」= silent fallback 禁止の配布版。

---

## Part 3 — 先行着手 / Web 待ちの分割 (T01)

- **先行着手可 (M4-01/M4-02 完了前)**: 5 プラットフォーム evidence (Windows/macOS/Linux/iOS/Android = Part 5 の該当行) + CDN 材料 (Part 4 の CDN 節 / `cdn-selection-material.md`)。
- **Web trunk 完了待ち**: Web 行 (Part 5 Web) + owner Web 実機 (T11) + CDN 選定 (T12) + 最終 sign-off (T13)。Web trunk は **M4-13 (Vulkan 完成) → M4-01 (WebGPU) → M4-02 (Unity WebGL) → M4-11** の直列。

---

## Part 4 — 確認 matrix サマリ (T02–T07)

status 凡例:
- `✅` — evidence anchor 実在 (CI job / CD job / 実機記録が repo に存在)。
- `🟡` — 部分 (cross-build のみ / required 非昇格 / weekly-nightly のみ)。honest な到達点であり達成偽装ではない。
- `❌` — gap (evidence なし)。owning WP / issue を gap flow 列に明記。
- `⏳` — owner 記入枠 (実機テスト / sign-off 待ち)。

| プラットフォーム | (i) CI 検証 (NFR-MT-02) | (ii) 配布パッケージ (NFR-DS-03) | (iii) 実機検証記録 | 既知 gap | gap flow 先 |
|----------------|------------------------|-------------------------------|-------------------|---------|-------------|
| **Windows**    | ✅ Tier 1 per-PR        | 🟡 CI artifact のみ (release 自動発行 job なし) | ✅ M0/M1 実機 | desktop release 自動発行 gap | T13 (owning WP 不在) |
| **macOS**      | ✅ Tier 1 per-PR + metal | 🟡 CI artifact + UPM/PyPI/XCFramework 内 (release 自動発行 job なし) | ✅ M0/M1 実機 | desktop release 自動発行 gap | T13 (owning WP 不在) |
| **Linux**      | ✅ Tier 1 per-PR + cuda build + musl | 🟡 CI artifact + musl 静的 server (release 自動発行 job なし) | ✅ M0/M1 実機 | desktop release 自動発行 gap | T13 (owning WP 不在) |
| **iOS**        | 🟡 per-PR cross-build (実機 nightly 未達) | ✅ XCFramework + Swift Package CD | ⏳ owner (iOS 実機 RTF、M2 carry-over) | 実機 RTF 未記録 / Tier 2 実機 nightly 未達 | owner backlog (M2 carry-over) + T13 |
| **Android**    | 🟡 per-PR cross-build + weekly (実機 nightly 未達) | ❌ standalone AAR なし (.so は UPM/Godot 同梱のみ) | ⏳ owner (M3-18 Android RTF) | AAR 配布物 gap / Tier 2 実機 nightly 未達 | T13 (owning WP 不在) + M3-18 (owner) |
| **Web (WASM)** | 🟡 `web-wasm.yml` (wasm-build / whisper-wasm-e2e / webgpu-smoke) + `nightly-webgl.yml` (Unity WebGL) — dispatch+weekly+path-PR、browser 実機は owner | ✅ `web/pkg` npm + `release.yml#npm-web-release` CD (Release tarball auto、registry は opt-in) + Unity WebGL staticlib in UPM | ⏳ owner (T11 Web 実機) | browser 実機未記録 / CDN 未選定 / registry publish opt-in 未発行 | T11/T12 (owner)。M4-01/M4-02 CC-side は land 済 |

> 各セルの evidence anchor と根拠は Part 5 の per-platform 詳細節に記載する。要約セルの色は詳細節の anchor 実在に従属する (`scripts/check-platform-support.sh` が anchor を機械検証)。

---

## Part 5 — プラットフォーム別 evidence 詳細 (T02–T07)

各セルは `evidence anchor 付き緑` か `gap 理由 + flow 先付き赤` のどちらか (空欄・根拠なし緑を作らない)。anchor 行 (HTML コメント、token は `path` または `path#job`) は `scripts/check-platform-support.sh` (T08) が実在検証する。

### 5.1 Windows / macOS / Linux (デスクトップ 3 OS、T02)

**(i) CI 検証 — ✅ Tier 1 per-PR 達成**:
- 3-OS build matrix `[ubuntu-latest, macos-latest, windows-latest]` で `cargo build --release` + per-OS capi cdylib upload (`libvokra.dylib` / `vokra.dll` / `libvokra.so`、retention 7 日)。
  <!-- anchor: .github/workflows/ci.yml#build -->
- 3-OS `cargo test --workspace`。
  <!-- anchor: .github/workflows/ci.yml#test -->
- rustfmt / clippy (`-D warnings`) — ubuntu。
  <!-- anchor: .github/workflows/ci.yml#fmt -->
  <!-- anchor: .github/workflows/ci.yml#clippy -->
- GPU feature gate: macOS = `metal` (Apple silicon runner で実 GPU parity 実行) / Linux = `cuda` (build+clippy+test、GPU 無し runner ゆえ dlopen device test は clean skip)。
  <!-- anchor: .github/workflows/ci.yml#gpu-backends -->
- **Linux GPU 第 2 系統 = Vulkan (M4-13、CC-side land 済 / 実 `.spv` は owner glslc 待ち = partial)**: opt-in backend crate `crates/vokra-backend-vulkan/` (GLSL source 12 本 `kernels/glsl/*.comp`) + parity CI `gpu-vulkan-parity.yml` (lavapipe、weekly + dispatch、required 非昇格)。committed 実 SPIR-V は handcrafted `copy_f32` / `add_f32` の 2 本のみ、MatMul/Mul/Softmax 等は owner の glslc precompile (`kernels/precompiled/` は現在 README のみ = M4-13-T16 / M3-02 T14-T22) 待ち。Android GPU (§5.3) と共通の backend。
  <!-- anchor: crates/vokra-backend-vulkan/Cargo.toml -->
  <!-- anchor: crates/vokra-backend-vulkan/kernels/glsl/gemm_subgroup.comp -->
  <!-- anchor: crates/vokra-backend-vulkan/kernels/handcrafted/copy_f32.spv.rs -->
  <!-- anchor: crates/vokra-backend-vulkan/kernels/precompiled/README.md -->
  <!-- anchor: .github/workflows/gpu-vulkan-parity.yml#parity -->
- Linux `x86_64-unknown-linux-musl` 静的リンク検証 + `/health` smoke + runtime workspace 非汚染 (FR-SV-01 / NFR-DS-02)。
  <!-- anchor: .github/workflows/ci.yml#server-deployment -->
- server 3-OS cross-build (ubuntu musl / macOS aarch64 / windows msvc) + 4-API compat。
  <!-- anchor: .github/workflows/ci.yml#server-compat -->

**(ii) 配布パッケージ — 🟡 部分 (release 自動発行 gap = 赤セル)**:
- deliverables §3.2 行 1「単一共有ライブラリ + CLI バイナリ (musl 静的リンク版含む)」に対し、現状は `build` job の per-OS artifact (retention **7 日**) と `server-deployment` の musl 静的 `vokra-server` のみ。
- **GitHub Release への desktop 共有ライブラリ + CLI 自動発行 job が `release.yml` に無い** (4 job = ios-xcframework / unity-package-release / python-pypi-publish / godot-package-release)。→ **NFR-MT-08「リリース物は CI 自動ビルド・自動発行、手動配布しない」対照の gap = 赤セル**。owning WP 不在 → **gap flow T13**。
- 言語バインディング (Python wheel) は緑 evidence として付記 (プラットフォーム別形態ではなく言語バインディング列):
  <!-- anchor: .github/workflows/ci.yml#python-wheel-build -->
  <!-- anchor: .github/workflows/release.yml#python-pypi-publish -->

**(iii) 実機検証記録 — ✅**: M0 (macOS/Linux/Windows demo 3 点) + M1 の Mac/Win/Linux 実機テスト完了 (milestones §11 実測完了行、`docs/benchmarks/v0.5-device-benchmarks.md` 系)。
  <!-- anchor: docs/benchmarks/v0.5-device-benchmarks.md -->

### 5.2 iOS (T03)

**(i) CI 検証 — 🟡 per-PR cross-build (実機 nightly = Tier 2 未達)**:
- `ios-build` job (per-PR): rustup targets `aarch64-apple-ios` + `aarch64-apple-ios-sim` + `x86_64-apple-ios` → `scripts/build-ios.sh` → `scripts/verify-ios-xcframework.sh` (static-only + symbol whitelist) → `swift package describe`。
  <!-- anchor: .github/workflows/ci.yml#ios-build -->
  <!-- anchor: scripts/build-ios.sh -->
  <!-- anchor: scripts/verify-ios-xcframework.sh -->
- **per-PR クロスビルドであり実機 nightly (NFR-MT-02 Tier 2) ではない** — 到達 tier は Part 7 参照。

**(ii) 配布パッケージ — ✅ CD 自動発行あり**:
- deliverables §3.2 iOS 行「静的ライブラリ (`DllImport("__Internal")` 前提)、Swift Package」に対し、`ios-xcframework` release job (XCFramework zip + sha256 + `Package.swift` binaryTarget patch) が CD 自動発行。
  <!-- anchor: .github/workflows/release.yml#ios-xcframework -->

**(iii) 実機検証記録 — ⏳ owner 記入待ち**: iOS 実機 RTF は **M2 carry-over の owner 残タスク** (NFR-PF-03 = iOS Whisper base RTF < 0.5、runbook `docs/m2-14-ios-rtf-handover.md`)。owner backlog と束ねる。**本 WP は新規実機チケットを起票しない** (既存 backlog の重複起票禁止)。
  <!-- anchor: docs/m2-14-ios-rtf-handover.md -->

### 5.3 Android (T04)

**(i) CI 検証 — 🟡 per-PR cross-build + weekly (実機 nightly = Tier 2 未達)**:
- `unity-package` job 内の Android cross-build (`android-actions/setup-android@v3` + `scripts/build-android.sh` → arm64-v8a `libvokra.so` を UPM `Plugins/Android/libs/arm64-v8a/` へ)。
  <!-- anchor: .github/workflows/ci.yml#unity-package -->
  <!-- anchor: scripts/build-android.sh -->
- `godot-crossbuild.yml` の `aarch64-linux-android` leg (5-target matrix、weekly + dispatch)。
  <!-- anchor: .github/workflows/godot-crossbuild.yml#crossbuild -->
- **Android GPU backend = Vulkan (M4-13、partial)**: §5.1 Linux GPU 第 2 系統と同一 crate `crates/vokra-backend-vulkan/`。実 op `.spv` は owner glslc + Android 実機 soak (M3-18 併走、lavapipe CI は driver-level bug を catch できないため実機 soak が exit hard gate) 待ち。anchor は §5.1 に集約。
- per-PR クロスビルド + weekly であり実機 nightly ではない (到達 tier = Part 7)。

**(ii) 配布パッケージ — ❌ standalone AAR gap = 赤セル**:
- deliverables §3.2 Android 行「AAR (`persistentDataPath` 展開ヘルパー内蔵)」に対し、**standalone AAR を生成する job は存在しない** (`bindings/` は python / unity のみ。arm64 `.so` は Unity UPM / Godot addon への**同梱のみ**)。
- NFR-DS-03 も「Android AAR」を列挙する**要件由来の gap** であり、**M4-01〜M4-20 に owning WP が無い** → **gap flow T13** (M4 内 follow-up / M5 送り / 配布形態要件の改訂の 3 択)。
- `persistentDataPath` 展開ヘルパーは Unity 側 `VokraAndroidAssets.EnsureLocalCopy` として実装済 (`nightly-il2cpp.yml` 冒頭 D6 コメント) = AAR 化時に流用可能な資産として付記。
  <!-- anchor: .github/workflows/nightly-il2cpp.yml#il2cpp-linux-headless -->

**(iii) 実機検証記録 — ⏳ owner 記入待ち**: M3-18 (owner、Whisper base RTF < 0.7 = NFR-PF-06、runbook `docs/m3-18-android-rtf-handover.md`、記入先 `docs/benchmarks/v0.9-device-benchmarks.md`)。既存 backlog、重複起票しない。
  <!-- anchor: docs/m3-18-android-rtf-handover.md -->
  <!-- anchor: docs/benchmarks/v0.9-device-benchmarks.md -->

### 5.4 Web (WASM) (T07)

> **統合 HEAD `0691ae9` で M4-01 (WASM/WebGPU/npm) + M4-02 (Unity WebGL) は CC-side land 済** (Part 0.2)。初版の「全面 gap」は古い base HEAD 由来の誤記録で、下記へ更新した。**anchor 行は実在 job / ファイルにのみ付す** (drift check が偽陽性を出さない設計)。owner の browser 実機 (T11) + CDN 選定 (T12) は honest に gap のまま残す。

**(i) CI 検証 — 🟡 CC-side leg land 済 (browser 実機 = owner)**:
- WASM/WebGPU build + smoke leg `web-wasm.yml`: `wasm-build` (2-artifact simd128/base wasm build)、`whisper-wasm-e2e` (Whisper base WASM e2e、dispatch opt-in の多 GB HF download)、`webgpu-smoke`。トリガは workflow_dispatch + weekly cron (Mon 07:00 UTC) + PR path filter (`crates/vokra-backend-webgpu/**` 他)。
  <!-- anchor: .github/workflows/web-wasm.yml#wasm-build -->
  <!-- anchor: .github/workflows/web-wasm.yml#whisper-wasm-e2e -->
  <!-- anchor: .github/workflows/web-wasm.yml#webgpu-smoke -->
- Unity WebGL build + headless browser smoke `nightly-webgl.yml` (`preflight` / `wasm-harness` / `unity-webgl-build`、nightly cron、`UNITY_LICENSE` secret gated = 未 provisioning 時 WARNING skip)。
  <!-- anchor: .github/workflows/nightly-webgl.yml#unity-webgl-build -->
  <!-- anchor: .github/workflows/nightly-webgl.yml#wasm-harness -->
- WebGPU backend crate 本体 + WASM SIMD dispatch: `crates/vokra-backend-webgpu/` (probe / plan / eval / 11 WGSL kernel)。
  <!-- anchor: crates/vokra-backend-webgpu/Cargo.toml -->
  <!-- anchor: scripts/build-wasm.sh -->
- **到達 tier は Tier 2 (WASM) 相当だが weekly + path-PR + dispatch であり per-PR-required (Tier 1) ではない**、browser 実機 nightly は owner lab (X-06) 待ち。→ 到達点は Part 7 に honest 記録。

**(ii) 配布パッケージ — ✅ CD job land 済 (registry publish は owner opt-in)**:
- deliverables §3.2 Web 行「WASM (+SIMD/Threads) + WebGPU、npm パッケージ」に対し、npm パッケージ足場 `web/pkg/package.json` + CD job `release.yml#npm-web-release` (M4-01-T23) が land。tag build で GitHub Release tarball + SHA-256 を auto 発行 (baseline channel、NFR-MT-08 対応)、registry.npmjs.org への publish は `secrets.NPM_TOKEN` gated opt-in (M4-01-T27、owner)。→ Unity UPM の OpenUPM opt-in と同型の posture。
  <!-- anchor: web/pkg/package.json -->
  <!-- anchor: .github/workflows/release.yml#npm-web-release -->
- Unity WebGL target: `wasm32-unknown-emscripten` staticlib を `scripts/build-unity-webgl-lib.sh` でビルドし UPM `Plugins/WebGL/` に配置 (M4-02、Unity 同梱 Emscripten がリンク)。
  <!-- anchor: scripts/build-unity-webgl-lib.sh -->
  <!-- anchor: bindings/unity/com.vokra.unity/Plugins/WebGL/libvokra.a.meta -->
- Web 入門手順 `docs/tutorials/web.md` (+ `web.ja.md`) を land 済。
  <!-- anchor: docs/tutorials/web.md -->

**(iii) 実機検証記録 — ⏳ owner (T11)**: Part 10 の Web ブラウザ実機テスト枠。owner が実ブラウザで Whisper base 動作 (NFR-PF-08) + Unity WebGL デモ動作 + WebGPU 非対応時の明示エラー / WASM-SIMD fallback (FR-EX-08) を記録する。**CC-side の CI/配布 leg は緑化したが、実機記録が無い限り Web 行は完全緑にしない** (fabricated pass 禁止)。

**方式非依存の記録原則 + zero-dep**: M4-01 の実装方式 (M4-01 T01 ADR で確定) には本行は言及せず **outcome のみ** 記録する (spec red-line)。**zero-dep 不変条件 (NFR-DS-02) は M4-01/M4-02 land 後も green** — `release.yml#npm-web-release` は `git diff --exit-code Cargo.lock` tripwire を内蔵し、`scripts/check-zero-deps.sh` は統合 HEAD で green (2026-07-15 再確認)。
  <!-- anchor: scripts/check-zero-deps.sh -->

---

## Part 6 — 付録: 判定対象外の補助 evidence

6 プラットフォーム判定に**混ぜない**補助 evidence (NFR-PT-01 前段の 6 対象外)。存在確認のみ記録:

- **サーバ x86-64 / ARM64**: desktop Linux/Windows と CI 上同一 evidence で覆う (`build` / `server-deployment` / `server-compat`)。独立行は立てない (NFR-PT-01 は「+ サーバ (x86-64/ARM64)」を 6 プラットフォームの付随として扱う)。
- **RISC-V (RVV 1.0)**: `riscv-cross-build` (per-PR、`riscv64gc-unknown-linux-gnu` cross-build + emitted-asm RVV mnemonic 検証)。実機 RTF は owner follow-up (NFR-PT-02/03、判定対象外)。
  <!-- anchor: .github/workflows/ci.yml#riscv-cross-build -->
- **Unity Package (横断配布物)**: `unity-package` (per-PR、UPM 組み立て + 構造 lint + NVIDIA 非同梱 + deps 検査) / `unity-package-release` (CD)。
  <!-- anchor: .github/workflows/ci.yml#unity-package -->
  <!-- anchor: .github/workflows/release.yml#unity-package-release -->
- **Godot GDExtension (横断配布物)**: `godot-crossbuild.yml` 5-target matrix (macOS×2 / Linux / Windows / Android) + `godot-package-release` (CD AssetLib zip)。
  <!-- anchor: .github/workflows/godot-crossbuild.yml#package -->
  <!-- anchor: .github/workflows/release.yml#godot-package-release -->
- **IoT 階層 (Pi 3/4/5 / RK3588 / Jetson 等)**: NFR-PT-02/03 の Tier 1/2/3。実機 nightly は X-06 owner lab standup 前提 (Part 7、判定対象外)。

---

## Part 7 — CI matrix 到達 tier の対照 (T06)

**NFR-MT-02 原文**: Tier 1 (Linux/macOS/Windows × 主要 arch) 毎 PR / Tier 2 (iOS/Android/WASM/Pi 3/4/5/RK3588/Jetson 等**実機**) nightly / Tier 3 weekly community。各プラットフォーム行の到達 tier を **honest 記録** (達成偽装しない):

| tier | 目標 (NFR-MT-02) | 到達点 (実 CI) | 判定 |
|------|------------------|----------------|------|
| **Tier 1** | Linux/macOS/Windows × 主要 arch 毎 PR | `build` / `test` の 3-OS matrix が per-PR で稼働 | ✅ **達成** |
| **Tier 2** | iOS / Android / WASM / Pi 等**実機** nightly | iOS/Android は per-PR **クロスビルド** (実機ではない)。**WASM leg は land 済** (`web-wasm.yml` = weekly + dispatch + path-PR / `nightly-webgl.yml` = nightly、いずれも実機ではなく headless / build)。`nightly-il2cpp` / `nightly-webgl` は daily/nightly cron だが `UNITY_LICENSE` secret 不在時 WARNING annotation 付き **skip**。`godot-crossbuild` は weekly。 | 🟡 **実機 nightly 未達** (WASM CI leg は land、browser/device 実機は owner lab) |
| **Tier 3** | weekly community | `godot-crossbuild` (weekly) / `gpu-vulkan-parity` (weekly) / `gpu-cuda-rtf` (self-hosted、`check-runner-availability` gated) / `web-wasm` (weekly) / model parity 群 `parity-kokoro-real` / `parity-whisper-real` / `parity-csm-real` / `parity-moshi-real` / `parity-rvq-real` / `parity-utmos` (weekly cron / path-PR) | 🟡 稼働 (opt-in、required 非昇格) |

<!-- anchor: .github/workflows/gpu-vulkan-parity.yml#parity -->
<!-- anchor: .github/workflows/gpu-cuda-rtf.yml#measure -->
<!-- anchor: .github/workflows/parity-kokoro-real.yml -->
<!-- anchor: .github/workflows/parity-whisper-real.yml -->
<!-- anchor: .github/workflows/parity-csm-real.yml -->
<!-- anchor: .github/workflows/parity-moshi-real.yml -->
<!-- anchor: .github/workflows/parity-rvq-real.yml -->
<!-- anchor: .github/workflows/parity-utmos.yml -->

**Tier 2 実機 nightly 未達の前提**: 達成には owner の Tier 2 実機 lab standup (milestones §11「2026-08 以降 … 実機テスト機材の確保: Tier 2 nightly CI 実機 (iOS / Android / WASM / Pi 3/4/5 / RK3588 / Jetson 等 — NFR-MT-02)」行 = X-06。M2-14 self-hosted CUDA runner standup と同種の owner 残) が前提。本 WP は tier 未達を達成扱いにせず、「per-PR クロスビルド + 実機は owner 手動記録」という**現状到達点**を記録する。

**到達 tier 記録の意味 (owner 承認事項)**: M4-11 完了条件「CI matrix (NFR-MT-02) で確認」は「NFR-MT-02 の目標 tier に対する現在地が CI 上で確認できる」ことであり、**Tier 2 実機 nightly の完備を M4-11 の close 条件にしない** (それは owner lab standup 後の継続タスク)。この解釈は **T13 で owner 承認を得る** — 勝手に完了条件を緩めない。**承認されなければ Tier 2 未達は M4-11 未 close のまま owner backlog 直列**、という判定分岐を明記する。

**required checks (NFR-MT-07)**: ci.yml 冒頭コメント + 各 job docstring が intended required set (build / test / fmt / clippy / gpu-backends / parity / license / ios-build / unity-package / python-wheel-build) を記す。**実際の branch protection required 構成は GitHub 設定であり repo からは不可視** → owner 確認事項 (T13)。weekly/nightly/self-hosted 系 (godot-crossbuild / gpu-vulkan-parity / gpu-cuda-rtf / web-wasm / nightly-webgl / model parity 群) の required 昇格は連続 green 数週後の owner 判断 (各 workflow の cool-off コメント)。**web-wasm は path-PR では走るが per-PR-required には未昇格** (HF download flakiness の PR blocking 回避、既存 parity workflow と同 posture)。

---

## Part 8 — 配布パッケージ列の対照 (T05)

NFR-DS-03 の配布形態列挙と deliverables §3.2 を突き合わせ、**「6 プラットフォーム判定に必要な形態」と「パッケージマネージャ流通」を分離**する (判定基準は deliverables §3.2 のプラットフォーム別形態、Part 1 定義)。

### 8.1 判定形態 (deliverables §3.2 プラットフォーム別) × 実在 CD job

| 判定形態 | 対象プラットフォーム | 実在 CD job / artifact | status |
|---------|---------------------|------------------------|--------|
| 単一共有ライブラリ + CLI (musl 静的含む) | Windows/macOS/Linux | `ci.yml#build` artifact (7 日) + `server-deployment` musl。**release 自動発行 job なし** | ❌ gap → T13 |
| 静的ライブラリ + Swift Package | iOS | `release.yml#ios-xcframework` (XCFramework zip + sha256 + Package.swift patch) | ✅ |
| AAR (`persistentDataPath` ヘルパー内蔵) | Android | **standalone AAR job なし** (.so は UPM/Godot 同梱のみ) | ❌ gap → T13 |
| WASM (+SIMD/Threads) + WebGPU + npm | Web | `release.yml#npm-web-release` (`web/pkg` pack → Release tarball + SHA-256 auto、registry publish は `NPM_TOKEN` opt-in) + Unity WebGL staticlib in UPM (M4-02) | ✅ (registry publish は owner opt-in) |
| 公式 Unity Package | Unity (横断) | `release.yml#unity-package-release` (+ OpenUPM は `OPENUPM_TOKEN` gated opt-in) | ✅ |
| 公式 GDExtension | Godot (横断) | `release.yml#godot-package-release` (AssetLib zip + sha256) | ✅ |

- Web の npm パッケージは M4-01 完了条件「npm パッケージが CD で自動発行される (NFR-MT-08)」の成果物 = `release.yml#npm-web-release` で **land 済** (GitHub Release tarball が baseline channel、registry.npmjs.org publish は `NPM_TOKEN` gated opt-in = owner)。Unity WebGL target は M4-02 で UPM `Plugins/WebGL/` staticlib として land 済 (§5.4 (ii))。**残 owner 分は registry publish の実発行 + browser 実機記録 (T11)** であって配布物そのものの実装 gap ではない。

### 8.2 パッケージマネージャ流通 (判定対象外の付録チャネル)

NFR-DS-03 は流通チャネルも列挙するが、M4-11 完了条件「配布パッケージ (NFR-DS-03) で確認」を**全 11 形態の完備と拡大解釈しない** (Part 1)。着手時期の割当は **owner 判断 (T13)** へ送る (owning WP 不在時の gap flow 規約どおり):

| 流通チャネル | 実在 job | 現状 |
|-------------|---------|------|
| PyPI wheel | `ci.yml#python-wheel-build` + `release.yml#python-pypi-publish` | ✅ (言語バインディング、緑) |
| npm (Web) | `release.yml#npm-web-release` | ✅ land 済 (Release tarball baseline、registry publish は `NPM_TOKEN` opt-in) |
| Cargo crate (crates.io) | — | 未整備 → T13 |
| Swift Package | (iOS XCFramework に内包) | ✅ (§8.1 参照) |
| Kotlin binding | — | 未整備 → T13 |
| vcpkg / Homebrew / winget / Debian 系 | — | 未整備 → **owner 判断 (T13) で着手時期を割当** |

---

## Part 9 — CDN 選定 (T12、owner)

CDN 選定材料は `docs/platform-support/cdn-selection-material.md` (T09、CC 作成の owner 記入式)。選定結果は本節に owner が記録する。

- **採用 CDN**: ______________ (owner 記入、T12)
- **選定根拠**: ______________
- **COOP/COEP レスポンスヘッダ制御可否** (WASM Threads 成立の必須要件): ☐ 可 / ☐ 不可 — ______________
- **費用見込み**: ______________
- **ドメイン接続** (`vokra.dev` / `vokra.io` / `vokra.ai`、未取得ならその旨): ______________

---

## Part 10 — Web ブラウザ実機テスト記録 (T11、owner)

M4-01 の Web 配布物 (WASM + WebGPU) を実ブラウザで動かし **Whisper base 動作 (NFR-PF-08 の v1.0-rc 到達分)** を確認する。ブラウザ組合せは owner 確定 ((提案) Chrome / Edge / Safari / Firefox × 各 OS)。WebGPU 非対応ブラウザで **silent に壊れていない** (明示エラー or WASM SIMD fallback、M4-01 設計) ことも確認 (FR-EX-08)。M4-02 Unity WebGL デモ動作も同一セッションで確認。NFR-PF-08 後段の Sesame CSM streaming は **M4-05/M4-06 owner 検収と合流** (本枠では重複確認しない)。

| ブラウザ | OS | WebGPU | Whisper base 動作 | Unity WebGL デモ | 気付き | Date |
|---------|----|--------|------------------|------------------|--------|------|
|         |    | ☐ 対応 / ☐ 非対応→明示エラー | PASS / FAIL |  PASS / FAIL |  |  |

**不動作組合せ (honest 記録)**: ______________

---

## Part 11 — WP 完了条件 / Kill switch G / owner sign-off

### 11.1 milestones §8 M4-11 完了条件チェックリスト (T10)

| # | 完了条件 | 状態 (統合 HEAD `0691ae9`) | 根拠 / 残 |
|---|---------|-----------------|-----------|
| 1 | 6 プラットフォームの official support が **CI matrix (NFR-MT-02)** で確認できる | 🟡 **一部** | desktop 3 OS = Tier 1 per-PR ✅ (Part 5.1)。iOS/Android = per-PR クロスビルド ✅ だが実機 nightly (Tier 2) 未達 (Part 7)。**Web = CI leg land 済 🟡** (`web-wasm.yml` + `nightly-webgl.yml`、weekly+dispatch+path-PR、browser 実機は owner T11、Part 5.4)。到達 tier の解釈 (Tier 2 実機完備を close 条件にしない) は T13 owner 承認事項。 |
| 2 | **配布パッケージ (NFR-DS-03)** で確認できる | 🟡 **一部** | iOS/Unity/Godot/**Web npm** CD ✅ (Part 8.1、`release.yml#npm-web-release` land 済 = registry publish は owner opt-in)。desktop 共有ライブラリ+CLI の release 自動発行 ❌ + Android AAR ❌ = flow 先付き赤セル (owning WP 不在 → T13)。 |
| 3 | **CDN 選定が完了している** | ⏳ **owner** | CC は選定材料 (`cdn-selection-material.md`, T09) を準備済。選定は T12 (owner)。CC 側 close 条件ではない。 |

**CC 到達分**: 確認 matrix 完成 (Part 4–8) + drift check 機械化 (`scripts/check-platform-support.sh`, T08) + CDN 材料 (T09)。**owner 引き渡し 3 件**: T11 (Web 実機) / T12 (CDN 選定) / T13 (sign-off + gap flow 判断) = `docs/handoff/m4-11.md`。

### 11.2 Kill switch G 判定材料 (T10、四半期 Go/No-go review 向け)

Kill switch G の判定窓「**ORT Web + WebGPU が Whisper base RTF < 1.0** を実用速度で達成 (12–18 ヶ月以内)」は本フェーズ (v1.0-rc、暦月 2026-12〜2027-03) と重なり、四半期 Go/No-go review (NFR-MT-05、第 3 回 2027-04 で判定窓入り = milestones §14) で明示判定する。

- **自側 evidence (CC 整理分)**: 本 matrix の Web 行 (Part 5.4) + M4-01 が land する Web 実測 (Whisper base の WASM/WebGPU 動作、NFR-PF-08) + owner Web 実機記録 (Part 10、T11)。base HEAD 時点では Web 未 land ゆえ自側実測は空 — M4-01 完了 + T11 記録後に埋まる。
- **対抗比較は owner review の判定事項**: 「ORT Web + WebGPU が RTF < 1.0 を達成しているか」の競合実測は **owner が四半期 review で確認**する。**CC は競合実測値を出典なしに書かない** (ハルシネーション厳禁)。本節は自側 evidence の所在整理までが CC スコープ。
- **判定の含意**: Vokra の Web (WebGPU) が Whisper base で実用速度に到達し、かつ ORT Web が同等未達なら Vokra の Web 差別化が生きる (No-go 回避)。ORT Web が先に到達したら Kill switch G 発動検討 (milestones §14 第 3 回 review)。

### 11.3 残課題の申し送り (T10 → owner)

`docs/handoff/m4-11.md` に集約。要点:

- (a) **T11 / T12 / T13 = owner** (Web 実機 / CDN 選定 / sign-off + gap flow 判断)。**Web の CC-side (WebGPU crate + web-wasm/nightly-webgl CI + npm CD) は統合 HEAD で land 済**ゆえ T11 (browser 実機) は now actionable — M4-01/M4-02 の land 待ちではない。
- (b) **Android AAR gap** (owning WP 不在、Part 5.3 / 8.1) → T13 の 3 択。
- (c) **desktop 共有ライブラリ+CLI の release 自動発行 gap** (NFR-MT-08 対照、Part 5.1 / 8.1) → T13 の 3 択。
- (d) **Tier 2 実機 nightly 未達** (Part 7) → owner lab standup (X-06) 後の継続タスク、T13 で解釈承認。
- (e) **milestones §8 M4-11 行の「(依頼者タスク一覧 v1.0 GA)」参照の 1 語修正** → 正 = §11「2026-12〜2027-03 (改訂: M4/v1.0-rc close 目安)」行。v-label 再割当 #2 の追随漏れ、軽微、owner 判断 (gitignore local milestones の編集は owner 領分)。

### 11.4 owner sign-off (T13)

owner は確認 matrix 全行を review し、**M4 Exit criteria「全プラットフォーム (Windows / macOS / Linux / Android / iOS / Web) の official support が出揃っている (NFR-PT-01、M4-11 の確認記録)」の判定材料として承認**する (v1.0-rc close の「OSS 機能完成 (rc) 判定」への入力であり商用 GA 宣言ではない = v-label 規律)。

**gap flow 判断 (3 択、残存赤セルごとに確定)**:

| 残存 gap | owning WP | owner 決定 (3 択: M4 内 follow-up / M5 送り / 要件改訂) |
|---------|-----------|------------------------------------------------------|
| Android AAR | 不在 | ______________ |
| desktop 共有ライブラリ+CLI release 自動発行 | 不在 | ______________ |
| NFR-MT-02 Tier 2 実機 nightly 完備 | X-06 (owner lab) | ☐ 現在地記録で M4-11 close (継続タスク化) / ☐ 未達のまま M4-11 open 保持 |
| Web (WASM/WebGPU/npm + Unity WebGL) | M4-01 / M4-02 (CC-side land 済) | (Web 行 CI/配布 leg は緑化済。残 = T11 browser 実機 + T12 CDN + registry publish opt-in 発行 → 記録後に Web 行を完全緑化して本 matrix を再確認) |

- **official support 宣言 sign-off**: ☐ 承認 (M4 Exit criteria 判定材料として) — 署名/日付: ______________
- 承認済み matrix は **M4-12 (v1.0-rc ABI baseline 記録)** の前提入力 (milestones §8 M4-12 依存列「M4-01〜M4-11 + …」) として引き渡す。

---

## 改訂記録

- **2026-07-15 (初版・M4-11-T01)**: operational 定義 (3 evidence 列) + SoT 所在 (本ファイル、tracked public) + gap flow 規約 (3 択) + 先行/Web 待ち分割 + base HEAD honest snapshot を land。出典 = milestones §8 M4-11 行 / NFR-PT-01 / NFR-MT-02 / NFR-DS-03 / NFR-MT-07 / NFR-MT-08 / BR-02 / deliverables §3.2。**新規 ADR は作らない** (spec T01: 本 WP は既存要件 ID の確認であり新規設計判断を持たない — M4-01 の方式 ADR は M4-01 側)。evidence セルは Part 5 の per-platform 詳細節で anchor 付き充填 (T02–T07)。
- **2026-07-15 (統合 HEAD 実態更新)**: 初版 worktree が古い base HEAD `328e61b` から分岐していたため Web / Vulkan を「未 land」と誤記録していた件を、**統合 HEAD `0691ae9` (M4 Wave 1-3 land 済) の実態へ機械再確認して更新**。主な変更: (1) Part 0 を §0.1 (初版 base 観測、provenance 保持) + §0.2 (統合 HEAD 更新) に分割。(2) **Web 行**: (i) CI = 全面 gap ❌ → 🟡 (`web-wasm.yml` wasm-build/whisper-wasm-e2e/webgpu-smoke + `nightly-webgl.yml` unity-webgl-build/wasm-harness、weekly+dispatch+path-PR)、(ii) 配布 = ❌ → ✅ (`release.yml#npm-web-release` CD、registry publish は `NPM_TOKEN` opt-in)、(iii) 実機 = ⏳ owner (T11) のまま。WebGPU crate (`crates/vokra-backend-webgpu/`)・`web/pkg` npm・`docs/tutorials/web.md`・Unity WebGL staticlib を anchor 追加。(3) **Vulkan (M4-13)**: §5.1 Linux + §5.3 Android に GPU 第 2 系統として land 済を記録、ただし実 `.spv` は handcrafted copy/add 2 本のみ committed で MatMul/Mul/Softmax 等は owner glslc 待ち = **partial** を honest 明記。(4) Part 7 tier 表を実 workflow 一覧 (web-wasm / nightly-webgl / godot-crossbuild / gpu-vulkan-parity / gpu-cuda-rtf / model parity 6 本) へ更新。(5) default test baseline 1759 → **2340 passed / 0 failed / 4 ignored** (2026-07-15 `cargo test --workspace` 実測)。**owner-pending は依然 honest gap のまま保持** (browser 実機 T11 / CDN T12 / AAR / desktop release 自動発行 / Tier 2 実機 nightly / iOS RTF)。anchor は 30 → 拡張 (drift check `scripts/check-platform-support.sh` verify green で全 anchor 実在再確認)。**新規 ADR なし** (確認 WP)。
