# M4-11 handoff: all-platform official support confirmation + Web device verification

- **Producer WP**: M4-11 (CC = confirmation matrix mechanization + evidence collection + CDN selection material).
- **Consumer**: M4 Exit criteria / v1.0-rc close judgment (OSS 機能完成 rc = 全プラットフォーム対応) + M4-12 (v1.0-rc ABI baseline records this matrix as a prerequisite input).
- **Requirement**: NFR-PT-01 (全プラットフォーム対応前提) / NFR-MT-02 (CI matrix tier) / NFR-DS-03 (配布パッケージ) / BR-02 (+ NFR-MT-07/08, NFR-PF-08).
- **Boundary (must not be misread)**: M4-11 is a **confirmation / record WP, not an implementation WP**. It does NOT implement gap fixes; gaps flow to their owning WP or to owner judgment (T13). It records the "全プラットフォーム対応" evidence for v1.0-rc close — it is **not** a commercial GA declaration, C ABI freeze, or semver start (freeze fires at M5-13 / v1.0 GA tag — v-label 再割当 #2).

## (0) base HEAD reality vs the WP premise (honest snapshot)

The matrix was built against the **actual** repo (`main`-relative, base HEAD `328e61b` = M3-terminal + docs), machine-verified, not against an assumed "Wave 1-3 integrated" state:

- **Web (WASM / WebGPU / npm) build/CI/実機 leg = zero** in this base. No wasm/webgpu job in `.github/workflows/`, no `web/` dir, no npm package (Unity UPM `package.json` aside). Source `wasm` refs are only future `cfg(target_family="wasm")` guards + README roadmap ("WebGL is planned for M4-02"). → **Web row = full gap**, owning WP = M4-01 (WASM/WebGPU/npm) + M4-02 (Unity WebGL).
- **Default test baseline = 1759 passed / 0 failed** (not 2340), consistent with M4 Wave 1-3 **not** being in this base. This WP adds only markdown + one shell script, so the Rust build is unaffected.
- Fabricated-pass is forbidden: the matrix records Web (and AAR / desktop-release / Tier 2 nightly) as honest gaps rather than showing everything green.

## (a) What CC completed

- **`docs/platform-support/v1.0-rc-support-matrix.md`** — the confirmation matrix (tracked public). Operational "official support" definition (3 evidence columns), gap-flow rules, 先行/Web split, 6-platform summary + per-platform detail (Part 5) with machine-readable anchors, CI-tier honest record (Part 7), distribution cross-reference (Part 8), owner-fillable Web-device (Part 10) + CDN (Part 9) + sign-off (Part 11.4) sections.
- **`scripts/check-platform-support.sh`** — zero-dep drift tripwire; every `<!-- anchor: path[#job] -->` in the matrix must resolve (file exists; YAML job id exists). Modes: verify / `--list` / `--self-test` / `--help`. Wired as an **advisory** `continue-on-error` step in the `license` job of `.github/workflows/ci.yml`.
- **`docs/platform-support/cdn-selection-material.md`** — owner-fillable CDN requirements checklist (R1–R6, COOP/COEP as mandatory gate) + candidate scaffold. CC does not select.
- No new ADR (spec T01: confirmation of existing requirement IDs, not a new design judgment). No Rust code touched; root `Cargo.lock` unchanged (NFR-DS-02).

## (b) Owner tasks

1. **T11 — Web ブラウザ実機テスト** (dep: M4-01, M4-02, matrix Part 5.4/Part 10). Run the M4-01 Web build (WASM+WebGPU) in real browsers; confirm **Whisper base works (NFR-PF-08)**. Browser set is owner-chosen (proposed Chrome/Edge/Safari/Firefox × each OS). Confirm WebGPU-absent browsers fail **explicitly** (or WASM-SIMD fallback per M4-01 design) — not silently (FR-EX-08). Confirm M4-02 Unity WebGL demo in the same session. NFR-PF-08's Sesame CSM streaming is the M4-05/M4-06 owner acceptance — do not re-verify here. Record into matrix Part 10.
2. **T12 — CDN 選定** (dep: T09, T11). Use `cdn-selection-material.md`. COOP/COEP header control is a mandatory gate (WASM Threads). Record採用 CDN + rationale + COOP/COEP method + cost + domain into matrix Part 9.
3. **T13 — 確認 matrix 最終承認 + official support 宣言 sign-off** (dep: T05–T08, T11, T12). Approve as the M4 Exit-criteria judgment material. Decide the gap-flow 3-choice for each residual red cell (Part 11.4 table). Hand the approved matrix to M4-12.

## (c) Residual gaps + flow (T13 決定対象)

- **Android AAR** — deliverables §3.2 / NFR-DS-03 list "Android AAR" but no CD job produces a standalone AAR (arm64 `.so` only bundled into Unity UPM / Godot addon). **No owning WP in M4-01..M4-20.** → 3-choice (M4 follow-up / M5 / requirement revision). `VokraAndroidAssets.EnsureLocalCopy` is reusable when AAR-ifying.
- **Desktop shared-lib + CLI release auto-publish** — `ci.yml#build` uploads per-OS cdylibs (7-day retention) but `release.yml` has no desktop-lib/CLI publish job (only ios/unity/pypi/godot). NFR-MT-08 対照 gap. → 3-choice.
- **NFR-MT-02 Tier 2 実機 nightly** — iOS/Android are per-PR cross-build (not real-device); `nightly-il2cpp` is UNITY_LICENSE-gated skip; `godot-crossbuild` is weekly. Real-device nightly needs owner Tier 2 lab standup (X-06). → approve "record current state, M4-11 closes; completing the real-device nightly is a post-standup continuing task", OR keep M4-11 open until achieved (Part 11.4).
- **iOS 実機 RTF** — M2 carry-over owner (NFR-PF-03), runbook `docs/m2-14-ios-rtf-handover.md`. Not re-ticketed here (no duplicate).
- **Web** — owning WP exists (M4-01/M4-02); differ back, green the Web row after they land, then T11.

## (d) One-word milestones参照 fix (owner)

`docs/milestones.md` §8 M4-11 row ends with "(依頼者タスク一覧 v1.0 GA)" — a stale reference from the 2026-07-08 mid-form (M4 = v1.0 GA). Post-v-label-reassign-#2 the correct reference is §11 "2026-12〜2027-03 (改訂: M4/v1.0-rc close 目安)" row (実機テスト (Web ブラウザ実機) + CDN 選定). Minor; owner edits the gitignore-local milestones (owner領分).

## (e) Kill switch G material

Judgment window "ORT Web + WebGPU achieves Whisper base RTF < 1.0 (12–18 months)" overlaps this phase; quarterly Go/No-go review 3rd (2027-04) enters the window (milestones §14). CC-side evidence = matrix Web row + M4-01 Web 実測 (NFR-PF-08) + T11 record (empty until M4-01 lands). **Competitor comparison is the owner's review call — CC does not write competitor numbers without a source.** See matrix Part 11.2.

## Cross-refs

- `docs/platform-support/v1.0-rc-support-matrix.md` (the matrix; Part 11 = completion checklist + Kill switch G + sign-off).
- `docs/platform-support/cdn-selection-material.md` (T09).
- `scripts/check-platform-support.sh` (T08 drift check; run `--self-test` then default verify).
- `docs/tickets/m4/M4-11-all-platform-support.md` (spec).
- `docs/milestones.md` §8 M4-11 row / Exit criteria / §11 M4/v1.0-rc close row / §14 review calendar (gitignore local).
- `docs/handoff/m4-12.md` (Consumer: v1.0-rc ABI baseline).
- Owner benchmark scaffolds: `docs/benchmarks/v0.9-device-benchmarks.md` (Android/Godot), `docs/m2-14-ios-rtf-handover.md`, `docs/m3-18-android-rtf-handover.md`.
