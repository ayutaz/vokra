# M5-02 QNN/Hexagon bakeoff report — 2026-08-24 prerequisite audit

**Verdict: INSUFFICIENT DATA / C ABI NO-GO for this freeze input.** This is not
a failed 2x performance run: no valid QNN run can be started on the current
host. It records the exact prerequisite gap so an absent SDK or Hexagon device
cannot be mistaken for a benchmark result.

## 1. Host and SDK fingerprint

| field | value |
|---|---|
| Date (UTC) | 2026-08-23 |
| Local date / timezone | 2026-08-24 / Asia/Tokyo |
| Host | iMac21,1, Apple M1, macOS 26.3 |
| Target Snapdragon device | not present |
| Hexagon HTP generation | not available |
| QNN / QAIRT SDK version | not installed |
| `QNN_SDK_ROOT` | unset |
| `QnnInterface.h` | not found under configured root, `/opt`, or `/Applications` |
| `qnn-net-run` | not found |
| `qnn-context-binary-generator` | not found |
| EULA/version sign-off | not supplied |

## 2. Execution-path state

`vokra-backend-qnn` currently provides an opt-in dynamic-library reachability
probe and explicit `BackendUnavailable` / `UnsupportedOp` errors. It does not
construct or execute a QNN graph. No QNN ABI function is called and no
whole-Whisper-encoder `DelegateBackend` implementation exists.

During this audit the earlier unverified placeholder `Qnn_Version_t` layout was
removed. A self-consistent guessed Rust struct is not evidence of compatibility
with a Qualcomm header. Callable FFI types will be introduced only from the
exact installed SDK headers with compiled size/alignment/offset probes.

## 3. Measurements

| field | value |
|---|---|
| Same-host CPU baseline | not run — there is no matched Hexagon host |
| QNN iterations | not run |
| HTP placement | not run |
| Numerical parity | not run |
| 2x speedup | not run |
| NFR-PF-12 verdict | **INSUFFICIENT DATA** |

No Apple-host CPU number is copied into this report. Doing so would violate the
same-machine/same-session requirement and create a fabricated denominator.

## 4. Required unblock sequence

1. Owner acquires and approves one exact Qualcomm AI Runtime SDK release,
   including its license/EULA and redistribution posture.
2. Supply its real `QnnInterface.h`, graph/tensor/backend headers, tools, and
   runtime libraries through `QNN_SDK_ROOT`; do not vendor them into Vokra.
3. Supply a Snapdragon device with a compatible HTP runtime and profiling
   access.
4. Implement header-derived provider negotiation, backend/device/context/graph
   lifetime, complete Whisper-encoder node/tensor lowering, finalize, execute,
   and teardown. Any missing op or HTP incompatibility must be an explicit
   error, never a CPU QNN backend substitution.
5. Establish CPU-oracle tensor parity before performance measurement.
6. Run alternating same-session CPU/QNN measurements and prove HTP placement
   `>= 0.90`; then record PASS or FAIL against 2x.

## 5. C ABI decision

**QNN selector: NO-GO with current evidence.** There is no executable delegate
and no real-hardware result, so adding or reserving a C ABI value would freeze a
fabricated contract. The combined CoreML/QNN selector remains absent from
`include/vokra.h`. Revisit as an additive minor-version API only after a real
QNN PASS/FAIL record exists.

Evidence:
`docs/bench-baselines/m5-02-qnn-bakeoff-2026-08-24/prerequisite-probe.txt`.
